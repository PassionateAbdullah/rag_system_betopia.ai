# Betopia RAG — full dependency set (MVP + production).
# Install: pip install -r requirements.txt
# For minimal MVP only, see pyproject.toml extras.

# ---------- core ----------
qdrant-client>=1.9.0
httpx>=0.27.0
python-dotenv>=1.0.0
pypdf>=4.0.0

# ---------- embeddings (local) + reranker ----------
sentence-transformers>=2.7.0

# ---------- ingestion (rich file types) ----------
markitdown[all]>=0.1.0

# ---------- postgres (canonical store + FTS) ----------
psycopg[binary,pool]>=3.1.0

# ---------- API server ----------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ---------- UI ----------
streamlit>=1.36.0

# ---------- dev / tests ----------
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.10.0
